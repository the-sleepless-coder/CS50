#Algorithms
implementing algorithms properly and efficiently

#Time complexity
O(n)
O(n/2)
O(logN)
The number of times a computer needs to calculate a certain algorithm

Roughly on the order of this algorithm
Running time of the algorithm
We only consider the highest order term
the biggest mathematic term is what we care about
when the n gets ininitely bigger we only consider the highest order

Being as efficient as possible as real world datasets gets bigger

O(N^2) : for loop(graph)
O(nlogn) : merge sort
O(n) : linear search
o(logn): binary search
O(1) : Hash set

best case or lower bound
small omega(lower bound)
big omega(higher bound)

theta
big omega && big o are the same

asymptotic annotation
how do these algorithms perform when n gets bigger

#Linear search
#Binary search
#Bubble sort
#Selection sort
#Call stacks
#Recursion
#MergeSort

#Data
In memory on top of the Circuit board
save data in each address
array : data stored back to back continguously

computer can look at one location at a time
needs to find a certain location methodically

#Searching
input -> algorithm -> bool 
array                 50 exists

#Linear search 
for( each door ){
    if 50 is behind door
        return true
}
return false

#Binary Search 

if no door left
    return false
if 50 is behind doors[middle]
    return true
else if $50 < doors[middle] ($75)
    search doors[0] ~ doors[mid-1]
else if $50 > doors[middle] ($20)
    search doors[mid+1] ~ doors[n-1]


# string의 길이 비교에 대한 확인
// strings are different lengths
// 시작하는 주소만 확인하는 것이기 때문에, 
// strcmp를 이용해서 모든 char가 같은지를 확인한다.
/*while (*s1 == *s2)
{
    if (*s1 == '\0')
        return 0;

    s1++;
    s2++;
}

각 주소지에 있는 값이 다 같은지를 확인한다.
모든 값이 다 일치한다면 0을 반환
다른 값이 발생하면 ascii 코드 상 차이를 반환한다. 
그래서 음수가 나오면 사전 순으로 앞
양수가 나오면 사전 순으로 뒤 
return *s1 - *s2; 
*/

#Sorting
What's the algorithm for sorting a random stream of numbers

unsorted -> algorithm -> sorted


When you sort a stream of numbers you do it because you want to reuse the sorted numbers
삽입
삭제
검색(값을 찾는 데 드는 시간복잡도)
조회(값 주소를 알고 조회하는 데 드는 시간 복잡도)

#Recursive
a function that calls itself

base case
find the answer and return the function 
found/0doors left

recursive case
you don't get the answer yet and need to recursively repeat the function until you find the problem
search left, search right

If no doors left
    return false
if number behind middle door
    return true
else if number < middle door
    search left half
else if number > middle door
    search right half


#MergeSort
If only one number
    return

else 
    sort left half of numbers
    sort right half of numbers
    merge sorted halves

n * log 2 n
log n to find out how many times the problems is divided into
and the number of times the numbers are ordered is n
hence n * log n


case 1
improve running time

increase the memory
increase the time

case 2
whop up a simple algorithm ruins running time
decrease the time


running time
vs
space
time
money